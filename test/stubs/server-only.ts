// Stub for the `server-only` marker package under Vitest (a non-RSC context).
// The real package throws on import outside a React Server Component; aliased to
// this no-op in vitest.config.ts so the data layer can be unit-tested.
export {};
