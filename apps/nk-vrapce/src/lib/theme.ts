/**
 * The club's brand colour as the PWA chrome bar renders it — a hand-kept mirror
 * of `--primary` in globals.css. Re-skin the club and update this in the same
 * change.
 */
export const THEME_COLOR = "#ffcb05";
